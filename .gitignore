# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Datos crudos y caché — nunca se versionan (repo público, datos de la API oficial y de terceros)
data/raw/
data/cache/

# Entorno local
.env
.env.produccion

# Spec de trabajo — decisión del usuario: no se sube a este repo público por ahora
specs/

# Editor / OS
.DS_Store
.vscode/
